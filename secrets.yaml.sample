# ESPHome Secrets File Template
# Copy this file to secrets.yaml and fill in your actual values
# Keep secrets.yaml secure and don't commit it to version control

# WiFi credentials
wifi_ssid: "YOUR_WIFI_SSID"
wifi_password: "YOUR_WIFI_PASSWORD"

# Fallback Access Point
fallback_ap_ssid: "Deye-Fan-ESP32-Fallback"
fallback_ap_password: "YOUR_FALLBACK_PASSWORD"

# API and OTA passwords (set secure passwords)
api_password: "YOUR_API_PASSWORD"
ota_password: "YOUR_OTA_PASSWORD"

